# 交接文档（2026-09-19 夜）

给**下一个代理**。当前对话套餐 token 即将耗尽。不要凭记忆发明状态；先读本文件，再读 `Agents.md`、`PROGRESS.md`、`README.md`。

本文件**不含密钥**。密钥只在本机 `D:\chatgpt\Czech_Uni_Apply\.env` 和 GitHub Repository Secrets。

---

## 1. 你是谁、仓库在哪、线上是什么

| 项 | 值 |
|---|---|
| 本地仓库 | `D:\chatgpt\Czech_Uni_Apply` |
| GitHub | `witold-andelie/Czech-Uni-Application` |
| 功能分支 | `codex/pipeline-czu-official-links-map`（与 `main` 基本同步） |
| `main` 在交接时的尖端 | `421f45a` 之后若已合并本交接提交，以 `git log origin/main -1` 为准 |
| 公网站 | https://czech-uni-application.com （Cloudflare Pages，**静态快照**，浏览器**没有** Supabase 密钥） |
| 活动快照 | **`v2026-09-19.1`**（`data/published/current.json`） |
| 公开岗位 | 快照里 15 条已审；`job-41000-1598` 官网 404，安全层已关窗 |
| UI 语言 | 仅 `zh-CN` / `en` / `cs` |
| 产品形态 | 纯浏览（A80），无账号/收藏/比较 |
| 本机没有 | `gh` CLI、GitHub PAT。不能代点 Actions。用 curl.exe 调公开 API。 |

所有者硬规则（违反即做错）：

- 留学项目必须先按**授课语言**分流；UI locale 不能变成授课语言偏好。
- 不编造岗位、工资、开放状态、录取率、覆盖数量。
- 不把学校首页或通用招聘列表当岗位公告。卡片标题用 `officialDetailUrl \|\| sourceUrl`。
- 不完整分页/抓取失败：**不关岗、不累加缺席、不改身份**。
- 覆盖声明默认 `not_asserted`。官方没写“全校都在此发布”就不能改。
- 三语审核通过才能进公开快照。英语不能顶掉缺失的中文/捷克文。
- 不绕过 Turnstile/登录/付费墙。不用 Google 翻译/Fonts 做核心 UI。
- 不买 Supabase Dedicated IPv4。Actions 只走 HTTPS Data API。
- 不把公告 HTML 全文写入 Postgres。
- 文档类台账：所有者习惯放本地 `docs/`（gitignored）。功能规则和 `registry` 引用的单位盘点可以进仓库。

开工必读：`README.md` → `docs/PRODUCT.md` → `docs/UI_RULES.md` → `docs/I18N_RULES.md` → `docs/DATA_MODEL.md` → `docs/CRAWLING_RULES.md` → `docs/ACCEPTANCE.md`。`docs/` 在 `.gitignore`，只在这台机器上。

---

## 2. 这一长会话实际做成了什么（按主题）

### 2.1 采集写入现用 Supabase（免费 HTTPS）

- 项目 ref：**`enpmwycjipwxqgwftksh`**（不是旧的 `blglgnrhnbidoujaitgy`）。
- GitHub Secrets（已由用户配置，本机读不到）：`SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`。空的 `SUPABASE_SECRET_KEY` 可以。
- Actions 上 `GITHUB_ACTIONS` 或 `SUPABASE_USE_REST=1` 时 `store_from_env()` 用 `RestStore`（`services/ingestion/src/storage/rest.py`），**不连** `db.*.supabase.co`。
- 迁移：`supabase/migrations/20260919120000_ingest_catalog_review_publish.sql`、`20260919130000_https_data_api_views.sql`（`public.ingest_source` 等视图，service_role 可写，anon 已 REVOKE）。
- 连通已在 Actions「Connect the collector to Supabase over HTTPS」多次 200。
- 用户控制台大约：Database Size **273.3 MB** / 免费上限 **500 MB**；Memory **406.51 MB** / Nano ~500 MB。磁盘 2 GB 很宽。更紧的是内存不是库容量。

### 2.2 免费库不爆的策略（已上 main）

- 身份 `catalog.research_job` 和事实 `research_job_version`：**永久留**。
- 每条来源只留最近 **14** 次 `ingest.source_run` 及其 `listing_observation`（`INGEST_KEEP_RUNS`）。
- 原文 HTML **不进库**，只存 SHA-256。
- 代码：`services/ingestion/src/storage/retention.py`，在 `ingest_source.py` 和 `ci_refresh.py` 结束时调用。
- 删除 run 前要把 `research_job_version.source_run_id` 和 `raw_document.run_id` 置空，否则外键挡住。

### 2.3 适配器引擎（不要再按学校复制一整份类）

| Parser | 类 | 行为 |
|---|---|---|
| `czu_wp_job_manager` | `CzuWpJobManagerAdapter` | WP 列表 + REST，行政岗也入库 |
| `cuni_ajax` | `CuniAjaxAdapter` | AJAX 分页 + `pracid` 详情，教学岗也入库 |
| 其他 HTML parser | `HarvestListingAdapter` | 薄封装 `discover_registered_candidates(followDetails=False)` 再抓详情 |
| `lmc_graphql`、`zcu_document_feed`、`detailFormat=pdf` | **无适配器** | 仍走旧 `harvest_nine_hei_jobs` |

入口：`adapter_for()`、`adapter_sources()` 在 `services/ingestion/src/adapters/jobs/__init__.py`。  
生产写入：`engine/run_source.py` + `engine/adapter_harvest.py`。  
单源 CLI：`python services/ingestion/src/cli/ingest_source.py --source-id <id> --live`。  
多源预算：`python services/ingestion/src/cli/ingest_adapters.py --live --budget-seconds 480`。

**注意：** `HarvestListingAdapter` 仍受底层 `parse_muni_vacancies` 等函数影响——马萨里克列表阶段会丢掉没有科研 track 的行。CZU/查理专用适配器不会在发现阶段丢掉教学/行政岗。

### 2.4 GitHub Actions `refresh.yml`（这是线上采集器，不是 Cloudflare）

- 定时：**每天 02:17 UTC 一次**，`mode` 空 → **full**（35 分钟预算，含项目目录和分片）。
- 手动 Run：**默认 `adapters`**（约 8–15 分钟，只跑 HTML 岗位适配器，跳过 129 个捷克语项目和分片）。
- 并发组：`official-source-refresh-${{ mode \|\| 'full' }}`，adapters 和 full **互不等待**。
- `cancel-in-progress: false`。不要让用户取消 `main` 上的定时任务去给功能分支让路。
- Playwright：用 setup-python 的 `python -m playwright`，**禁止** `sudo python`（会落到 `/usr/bin/python` 没有 playwright）。
- 动态页：`network_idle=False`，单页约 20s；缺 Chromium 则跳过浏览器。
- 适配器顺序：上次 `ingest-adapters.json` 的 `deferred` 优先，其次环境变量 `ADAPTER_PREFER_IDS`。artifact 名 `adapter-cursor`。
- 功能分支**禁止** `git push origin HEAD:main`。关闭状态只在 `main` 上提交 `data/published/safety`。
- 已开始任务的失败不再把整次 GitHub job `exit 1`（「Report recorded source retries」只打日志）。`tick_exit_code` 对混合复查 124/125 也不再失败。
- 本机**不能** `workflow_dispatch`。让用户打开：  
  https://github.com/witold-andelie/Czech-Uni-Application/actions/workflows/refresh.yml  
  分支 **`main`**，mode **`adapters`**（短）或 **`full`**（长）。

### 2.5 已用适配器跑过并核对过的库（抽样）

**CZU `czu-central-jobs`（msmt-vs_41000）**  
中央源当前约 5 条 `jobs.czu.cz/job/…`（1601–1605 量级；1605 行政岗已保留）。`job-41000-1598` 官网 404，安全层 generation 22 窗口关闭。单位盘点：`data/sources/coverage/czu-unit-inventory.json`（17 个官方结构单位，中央源 **not_asserted**）。

**查理大学 `cuni-central-open-positions`（msmt-vs_11000）**  
适配器一次 listed/parsed **8**，详情全是 `https://cuni.cz/UKEN-1573.html?pracid=…`。含讲师岗。库里该校还有旧 persist 的列表页身份（MFF `/job-opportunities`），缺席计数 2，未关闭。单位盘点：`data/sources/coverage/cuni-unit-inventory.json`（17 学院 + 校长办）。官方 https://cuni.cz/UK-15765.html：**多数岗位发在学院网站**，故 **not_asserted**。4 个大学研究所未在该页点名，`unresolved` 里写了，不要编造名单。

**马萨里克 `muni-careers`（msmt-vs_14000）**  
adapters #32：5/5，远程 ID `81369, 81445, 81652, 81700, 81701`，URL `…/vacancies/<id>`。`parse_muni` 仍丢非科研行。

**adapters #32**（run 35463290925，9.5 分钟）：先跑了 15 个源，UJEP 13 列出/10 解析（不完整，未关岗），另 15 个 deferred。

**adapters #33**（run 35464234943，SHA `421f45a`，8.5 分钟）：deferred 15 个全部跑完。OSU 11、POLAC 8、VSTE/VŠPJ/AMU 各 3、PEUNI 1；SLU/VETUNI/UNOB/AVČR/AVU/JAMU/MUP/MVSO/UNYP listed=0（完整空列表或 parser 与页面不合，**不能写成该校没有岗位**）。然后顺带重跑了 CUNI/MUNI/CZU/CTU。这次轮空：JCU、UPCE、UTB、VŠE、Mendelu、UJEP、UHK。

离线探测：54 所高校，**31** 所有启用招聘源，**23** 所缺口。脚本：`python services/ingestion/src/cli/probe_job_sources.py`（默认不上网）。报告 `work/runs/job-source-probe.json` 不提交。

### 2.6 公开岗位深链（快照 v2026-09-19.1）

- D3S 博士后已改到 `https://www.d3s.mff.cuni.cz/positions/2026-1/`，重审绑定新哈希，已发布。线上页面含 `2026-1`。
- 其余公开岗：MUNI 数字 ID、CZU WP slug、CUNI `pracid`、JCU PDF、ZCU Alfresco，均为公告页。审计 JSON **不要进 git**，本地副本可在 `docs/public-job-official-urls.json`。
- 发布校验拒绝列表页当公告；`v2026-09-17.1` 对 D3S 旧列表 URL 有祖父豁免。
- `UKEN-1573.html` **没有** `pracid` 算列表；有 `pracid` 算公告（`engine/urls.py` `vacancy_query_id`）。

### 2.7 地图（已上线）

- **默认**项目内置捷克 SVG 详图（14 州 + 城市 + 水系），**不**等 OSM。国内可用。
- 街道图是可选按钮。
- 线上 `/zh-CN/map/`：`data-map-provider=czechia-svg`。
- 代码：`apps/web/src/components/InstitutionMap.svelte`。不要再把 OSM 当默认。

### 2.8 联系方式（已上线）

- 邮箱：`andelie1892@gmail.com`
- 微信：**`CZ_1892`**（复制按钮也是这个）。不要改回 `wxid_vtl13ojnba0d22`。
- 组件：`apps/web/src/components/AuthorContact.astro`。工作区里若再改微信/文案，那是所有者的，与采集分支无关；合并时不要丢掉，也不要夹带未完成的联系页实验，除非用户明确要。

### 2.9 未提交、不要乱提交

- `AGENT_RUNBOOK_SUPABASE_INGESTION_AND_MAP.md`：未跟踪。可作背景，执行以本交接和 `main` 代码为准（其中旧项目 ref 已过时）。
- 不要提交 `.env`、`work/`、`docs/`（被 gitignore）。

---

## 3. 未完成工作：按优先级做（怎么做写清楚）

### P0 — 不要做的事

1. 不要买 Dedicated IPv4。  
2. 不要把 HTML 全文写入 `ingest.raw_document` 的 body 列（现在也没有 body）。  
3. 不要自动把 `sourceCoverageClaim` 改成全覆盖。  
4. 不要不经三语审核打公开快照。  
5. 不要用 `sudo python -m playwright`。  
6. 不要在功能分支 `git push origin HEAD:main`。  
7. 不要把「listed=0」的学校写成没有岗位。  
8. 不要取消 `main` 上正在跑的 **full** 定时任务去给别的实验让路。

### P1 — 继续适配器收割（短，推荐）

再手动 Run **adapters**（`main`，mode=`adapters`）。游标会优先上次 deferred：JCU、UPCE、UTB、VŠE、Mendelu、UJEP、UHK。

看产物 `work/runs/ingest-adapters.json`（artifact `candidate-review-<runid>` 或 `adapter-cursor`）。

查库（本机，勿打印密钥）：

```text
py -3  # 加载 .env 后 GET
  ingest_source_run?source_id=eq.<id>&order=started_at.desc
  catalog_research_job?employer_id=eq.msmt-vs_<rid>
```

雇主 ID 例子：CZU `msmt-vs_41000`，查理 `msmt-vs_11000`，马萨里克 `msmt-vs_14000`。

### P1 — 23 所缺口的 `--live` 探测（要打高校，须用户同意）

```text
python services/ingestion/src/cli/probe_job_sources.py --live
```

只建议 parser + URL，**不要自动改 `registry.json`**。高置信、官方域、类型已有适配器时，再**一行行**人工/代理加 registry（`sourceCoverageClaim: not_asserted`）。然后下次 adapters 就会收。

### P2 — 还没进适配器引擎的 3 类源

- `vut-central-careers` / `lmc_graphql`（Jobs.cz 私有 API）
- `zcu-central-vacancies` / `zcu_document_feed`
- `tul-central-careers` / `detailFormat: pdf`

不要用 `HarvestListingAdapter` 硬套。需要专用 fetch（GraphQL POST 或 PDF 二进制）。

### P2 — 马萨里克教学岗

`parse_muni_vacancies` 在列表阶段丢无 track 行。若要「官方公告全收」，改的是 **parse_muni**，不是再写一个大学类。改完用夹具测试，禁止 live 测试进 pytest。

### P2 — 单位盘点

CZU、查理已有 JSON。马萨里克等第三所：对照官网结构页 + MŠMT `msmt-faculties.json`，中央源 **not_asserted**，未点名的研究所放 `unresolved`。

### P2 — 审核队列（增加公开岗数量，不扩大采集）

台账仍大约 **125 当前候选 = 15 公开 + 110 阻塞**（三语缺、薪酬未证实、证据变了，可重叠）。路径：`data/sources/coverage/candidate-disposition.json`。  
发布：`py -3 services/ingestion/src/publish.py --check-sources` 通过后 `py -3 services/ingestion/src/publish.py` 切新 `v2026-09-19.N`。必须绑定 `sourceHash`/`factHash`。合并 `main` 后等 `ci.yml` 的 Cloudflare Pages。

### P3 — 项目（与岗位分流）

授课语言第一层。CZU 英/捷/博士采集已在 **full** tick。捷克语约 129 页、每页 sleep 2s，看起来像卡死，其实要 8–12 分钟；进度日志已在 `harvest_czu_czech_programmes.py`。  
项目适配器探测尚未做。不要把科研岗筛选套到项目上。

### P3 — 查理大学 4 个研究所

官方介绍写了 17 学院 + 4 研究所 + 其他。盘点页 UK-8534 没点名研究所。补盘点时要找到**官方点名名单**再写，不要用维基。

### P3 — 地图深化

所有者只要捷克地区详细。SVG 已是默认。不要上 OSM/R2/PMTiles，除非用户再要街道级且自建瓦片。

---

## 4. 常用命令（Windows PowerShell）

```text
# 测试（仓库根）
py -3 -m pytest services/ingestion/tests/test_ingest_adapters.py services/ingestion/tests/test_retention.py services/ingestion/tests/test_cuni_adapter.py services/ingestion/tests/test_adapter_harvest.py services/ingestion/tests/test_registered_listing_adapter.py services/ingestion/tests/test_probe_job_sources.py -q

# 离线缺口
py -3 services/ingestion/src/cli/probe_job_sources.py

# 发快照（仅审核通过后）
py -3 services/ingestion/src/publish.py --check-sources
py -3 services/ingestion/src/publish.py

# 网站
cd apps/web
npm test
npm run build
npx playwright test tests/e2e/map.spec.ts tests/e2e/contact.spec.ts
```

合并到 main 的惯例：先 `git fetch`；不要带 `.env` 和 `AGENT_RUNBOOK_*`；冲突常见在 `README.md` 的 refresh 段落。

查 Actions：  
`https://api.github.com/repos/witold-andelie/Czech-Uni-Application/actions/workflows/refresh.yml/runs?per_page=5`  
产物：`https://nightly.link/witold-andelie/Czech-Uni-Application/actions/runs/<id>/candidate-review-<id>.zip`

---

## 5. 关键文件地图

| 路径 | 用途 |
|---|---|
| `.github/workflows/refresh.yml` | 采集（adapters / full） |
| `.github/workflows/ci.yml` | 测试 + Cloudflare Pages |
| `services/ingestion/src/adapters/` | 岗位适配器 |
| `services/ingestion/src/engine/run_source.py` | 完整/不完整门闩 |
| `services/ingestion/src/engine/urls.py` | 列表页 vs 公告页 |
| `services/ingestion/src/storage/rest.py` | Actions 写库 |
| `services/ingestion/src/storage/retention.py` | 14 次 run 上限 |
| `services/ingestion/src/harvest_nine_hei_jobs.py` | 旧单体，PDF/LMC 仍用 |
| `data/sources/registry.json` | 来源登记 |
| `data/published/current.json` | 活动快照指针 |
| `locales/*.json` | 三语；改完 `py -3 scripts/sync_locales.py` |
| `apps/web/src/components/InstitutionMap.svelte` | 捷克详图 |

---

## 6. 建议下一个会话怎么开场

1. `git fetch origin`，确认 `origin/main`，不要在过期功能分支上假设已部署。  
2. 读本文件 §3 P1。  
3. 若用户要继续收岗：让用户 Run **adapters**（你不能代点），再读 `ingest-adapters.json`。  
4. 若用户要更多学校：先 `--live` 探测 23 所缺口，再只登记已有 parser 的官方域。  
5. 若用户要更多公开岗：走审核队列，不要假装采集等于发布。

建议技能：无强制。改 UI 必须按用户规则做浏览器/Playwright 验证（手机+桌面）。采集禁止在 pytest 里打真实高校。
