# 项目执行规则

用户明确要求优先；本文件约束在本项目中工作的编码与采集代理。

## 开工必读

README.md → docs/PRODUCT.md → docs/UI_RULES.md → docs/I18N_RULES.md → docs/DATA_MODEL.md → docs/CRAWLING_RULES.md → docs/ACCEPTANCE.md。
OPM 已确认是 ISO 19450，采用当前 ISO 19450:2024。读 opm/README.md 与 opm/OPL.md；修改功能时同步更新对象、状态、过程关系和需求追踪。DOT 是 OPM 语义的显式映射，不声称经过正式 ISO 合规认证。

## 产品硬规则

- 留学项目必须先按授课语言分流，英语与捷克语并列为第一层。学校、学位、排名均不能替代第一层。
- UI locale 仅为 zh-CN / en / cs；授课语言使用独立 BCP 47 语言码。不得把中文 UI 自动变成捷克语或英语项目偏好。
- 所有页面，包括错误、空结果、登录、隐私、表单验证和后台交接涉及的用户提示，必须有三语方案。
- 禁止运行时依赖 Google 翻译、Google Fonts、境外在线翻译部件提供核心界面。核心搜索在屏蔽此类服务后仍须可用。
- 所有正式发布的核心项目与岗位信息都须完成三语版本；源文可附后，不能用英语静默替代缺失的捷克文或中文。
- 不编造数据、职位、工资、开放状态、录取概率、排名、赞誉、用户评价或覆盖数量。
- 岗位必须依据学历和博士注册要求全文判断，不以职位名或 R1 标签直接判定。

## 实施规则

- 保留工作语言条件与授课语言条件的边界，科研岗位不套用授课语言筛选。
- 优先实现能验收的完整流程：三语语言入口 → 列表筛选 → 详情 → 来源查看。产品为纯浏览，无收藏/比较/账户（2026-09-12 所有者决定 A80）。
- 用共享组件和 design/tokens.json 管理样式；禁止每页自行定义视觉体系。不得重新引入公开注册、收藏、比较或任何按用户存储。
- 优先实现能验收的完整流程按 A80 修订：纯浏览；本条为 A80 后的执行文本。
- 建议技术方案写在 ARCHITECTURE.md，技术栈版本在正式实施时核验并锁定。
- 性能选型按 LANGUAGE_STACK.md：TypeScript/Astro 静态页面与 Svelte 交互，Go 在线 API，Python/Scrapling 离线采集，SQL/PostgreSQL 数据层。不得把抓取和翻译放入在线请求链路。
- 难提取公开网页必须按 CRAWLING_RULES 尝试已安装的 Scrapling；不得用虚构内容填补抓取失败。
- 关键页面截图、功能测试和语言检查按 ACCEPTANCE 完成；没有实际验证的项目标“未验证”。
- 采集原文、日志与临时文件放 work/；正式发布数据放 data/published/；来源注册表放 data/sources/。
- 仅实施当前获授权范围。此框架不代表已获购买域名、收费服务或代投申请授权。

## 刷新与岗位发布硬规则

每隔五天（120 小时）全覆盖轮询官方来源，遵循 docs/REFRESH_POLICY.md。高校名录不能只收名校或只收有英语网站的学校。
确认关闭或截止的岗位自动下架并保留归档；岗位标题和主按钮直达官方申请目标，本站详情只是次级入口。
调度器属于部署后的 Go 后台任务；当前框架不得被报告成已开启自动采集。

## 学校参考与轮次

学校必须标公立／私立／国立及证据；中留服只发布有官方证据的参考状态，不保证个人认证。项目和岗位展示申请起止与官方已公布轮次；不把第一轮结束当作整个项目结束。执行 docs/RECOGNITION_AND_WINDOWS.md。
