# 捷克留学平台第三轮复审：仍需修改的事项

审计对象：`D:\chatgpt\Czech_Uni_Apply`，2026-09-07 当前未提交工作区；正式快照为 `v2026-09-07.2`。本轮只审计及运行隔离验证，没有修改业务代码、发布快照、启动真实采集或部署。

结论：改动方向正确，已有检查全部通过，但尚未满足“经审核快照是唯一正式数据源”和“地图经实测后替换”的完成条件。尤其不能依据 PROGRESS 中的“100%”描述直接验收。以下问题包含已复现缺陷及明确标注的代码路径问题。

## 已验证的改进

- Web 70/70、Python 49/49 通过；Go catalog 测试通过（缓存），Go API 的 vet 通过。
- TypeScript 检查通过。Astro check 在正常子进程权限下为 47 个文件、0 errors／warnings／hints；受限环境首次出现 picomatch 配置加载错误，本轮不将该环境差异当作未修复代码缺陷。
- 正式构建成功，15,314 页；专业列表浏览器显示 20 张卡片，无 pageerror。
- 已增加 published 导入、校验脚本、版本目录、调度状态、Windows 非破坏性 PID 检查、多轮合并和申请入口失败保留旧值。
- 先前“发布日期误作截止日期”、无关语言 not required 等简单样例的修复已落入代码。中文岗位标题补充和页面降噪也有进展。

以上说明基础检查改善，不代表下面的数据异常、并发和网络故障已经被现有测试覆盖。

## A31 · P1 · 采集仍能绕过发布快照，污染正式浏览器数据

位置：`apps/web/src/lib/loadCatalogClient.ts:28`；`services/ingestion/src/build_nine_hei_inventory.py:143–150`；`services/ingestion/src/worker.py:194–205,288–294`；`apps/web/scripts/verify-published.mjs`。

构建时 import 已改到 published，但浏览器依旧请求固定路径 `/data/nine-hei-inventory.json`。采集器仍直接写 `apps/web/public/data/`，该目录会被静态构建复制进 dist。校验器只检查 published/current 内的文件，完全不检查 public 数据。因此，只需先采集再构建，就可能让旧快照通过门禁，同时将新原始 inventory 送给浏览器，重新出现列表与详情版本混用。

修改要求：采集器只能写工作数据；构建开始选定一个不可变版本，在隔离构建目录中仅从该版本生成公开资源。客户端资源路径带版本号，或嵌入清单并严格匹配哈希。构建不读取可变的 public 工作副本。新增“采集改写 sources 后，不发布直接构建”测试，确认发布内容仍全部来自指定旧版本。

## A32 · P1 · 门禁验证的是文件字节与数量，未验证数据是否合格

位置：`services/ingestion/src/publish.py:45–91,218–241`；`apps/web/scripts/verify-published.mjs:41–61`。

隔离复现结果：

1. 只有 `manifest.json`，其中 `validation.passed=true`、`checksums={}`，没有任何目录数据，JS 验证器仍退出 0；Python verify_current 也返回通过。这不证明完整 Astro 构建一定成功，但证明所谓前置完整性门禁可空过。
2. 保持数量，在一个岗位里放入不存在的 employerId、空中文／捷克文、unreviewed 和非 HTTP 申请 URL，validate_sources 仍返回通过。异常 URL 仅为测试字符串，未执行或访问。
3. 当前合格快照实际包含 14 个 `translationStatus=unreviewed` 岗位。数量门禁没有执行项目要求的三语审核、资格证据与引用完整性。

修改要求：固定必需文件集合且不允许空清单；验证 manifest schema、发布状态、版本、严格布尔值、哈希路径边界、实际记录数量、唯一 ID、外键、窗口归属、来源证据、允许的 URL、薪酬／资格状态和同源版本的三语审校。数量是异常信号，不是业务真实性证明；固定要求永远恰好 54 所及至少 10 个岗位，也会错误阻挡合法基线变化或季节性减少，应改为有解释的变更门禁。

验收：空清单、缺一必需文件、伪造 counts、未知雇主、重复 ID、缺语种、未审校、危险 URL 都失败。只做规则校验不代表已人工核验事实，但不能连这些基础检查也省略。

## A33 · P1 · 发布和回滚未原子绑定一个版本

位置：`publish.py:103–213`。

代码路径：先校验 sources，再逐文件复制；校验与复制之间来源可被采集器修改。切换 current 是“旧目录改名→新目录改名→写 current.json→复制 public 文件”，中间存在 current 缺失及多资源不一致窗口。rollback 切换 current，却没有同步 public inventory，旧快照可以搭配新公开文件。发布未加互斥；正式构建也一直引用可变 current，而不是启动时固定的 snapshots/version。

修改要求：先复制到唯一 staging，再校验 staging；将完整成功产物作为不可变版本；构建解析一次 activeVersion 后固定绝对版本目录。以一个原子指针切换可见版本，旧版本保留。对失败、并发发布、构建中切换及回滚设计恢复测试。本轮未对真实 published 执行故障注入，此项依据代码路径。

`--force` 不应把未通过门禁的版本设为 current 并同步 public。JS 构建会拒绝 `validation.passed=false`，但该操作已破坏当前有效指针；Python `--check` 又只验哈希，会给出不同结论。失败候选应留在 staging，线上旧版本及 active 指针均不变。

## A34 · P1 · 每日岗位复核重新丢失多轮，并错误关闭整岗

位置：`services/ingestion/src/worker.py:240–294`。

分片合并修复了多轮，但 `recheck_open_jobs` 又按 ownerId 建单值字典，最终把该字典写回。隔离输入同一岗位两个窗口：active 截止 9 月 30 日、old 截止 9 月 1 日。在 9 月 7 日执行，结果只剩 old，岗位整体变 closed／archived，仍有效的 active 被删除。

修改要求：保留每个独立 window ID；逐轮判断，整岗是否关闭由所有适用轮次和官方整岗关闭证据决定。使用 Europe/Prague 及各窗口时区、日期精度，不用 UTC 日期代替布拉格业务日。验收包含两轮不同排序、一轮关闭另一轮开放、未来补录、时间戳精度和未知截止，任何排序都不能改变最终结论。

## A35 · P1 · 调度把抓取失败登记为成功，掩盖陈旧数据

位置：`worker.py:264–298,331–347`；`schedule.py:121–126,198–224`。

已复现：全部岗位请求返回 503，依然调用 record_job_recheck_success；分片报告 `ok=0, failed=1`，依然调用 record_shard_success。原因是只把抛异常视为失败，正常返回的错误状态不参与成功判断。

另有重试缺口：刚记失败且 retryAt 尚未到期，当天分片仍被 daily_scheduled 分支立即排入。失败状态又被 SLA 检查直接跳过，长期失败可能没有相应违约记录。持久状态目前主要按分片，不是逐来源维护；一个学校失败不能被同分片其他学校成功覆盖。

修改要求：区分 attempt、HTTP 成功、解析成功、核验成功、发布成功；失败不推进 lastSuccessAt。逐来源保存到期和失败信息，所有排队入口尊重 retryAt；失败中的来源仍统计 SLA。等待时长应参考最近任务／重试时刻，而非最多一小时轮询吞掉 5 分钟重试。验收涵盖全部失败、部分失败、解析为零、重试未到期、120 小时持续失败。

## A36 · P1 · 进程误杀已改，但互斥仍不是原子的

位置：`worker.py:102–122`；`schedule.py:240–267`。

RunLock 仍是 exists→读取→write_text，没有排他创建。隔离测试让两个执行者同时完成 exists 检查，两个均成功进入临界区。这里用线程确定性模拟检查和写入之间的交错，未干扰真实进程或真实锁文件。它说明存在同样适用于多进程的竞态。

ScheduleManager 的租约同样为读取状态后写回，没有事务；每日 recheck 路径也没有与普通采集共用全局互斥，可能互相覆盖岗位文件。单个 JSON 原子 replace 只能防止半文件，不能防止丢更新。

修改要求：采用可靠跨平台文件锁或数据库事务租约；明确租约拥有者、续期、过期回收及写入条件。所有修改同一目录数据的路径遵守同一互斥规则，补两个独立进程同时启动、抓取与复核交错的测试。

## A37 · P1 · 所谓动态发现尚未接入真实工作流

位置：`harvest_nine_hei_jobs.py:534,561,943–977,1193–1195`；`worker.py:143–145`。

`parse_muni_vacancies` 和 `parse_avcr_vacancies` 在生产代码中只有定义，调用来自测试；主入口仍只 harvest_candidates(VERIFIED_CANDIDATES)。MUNI 两岗位是新加的常量，并不是定时发现器发现后自动进入目录。当前 published 仍是 18 岗、5 雇主，没有 MUNI 或 AVCR。

修改要求：来源注册表真正驱动列表抓取、分页、详情发现、稳定 ID、资格解析和发布；增加一条不在任何常量中的合成新职位，证明运行入口能发现它。验证列表绝对／相对链接、捷克语页面、重复分页以及新职位消失的路径。AVCR 是机构集合，须辨认实际研究所雇主，不能永远用 avcr-central 代替。

完成定义应是“自动发现→详情核验→资格及三语审校→发布可见→复核关闭”，而不是“解析器单元测试存在”。

## A38 · P1 · 资格与关闭提取仍会跨岗位污染

位置：`harvest_nine_hei_jobs.py:323–332,349–407,478–531`。

新关闭判断找到标题后截取固定 1,500 字符，没有识别下一个岗位边界。已复现：开放岗位 B 后紧跟已关闭岗位 A，针对 B 仍返回 closed。标题若在顶部目录出现，也可能从错误位置截取。

已复现 `Enrollment in a PhD is not required.` 被判为 required：正向关键词早于否定语义判断。为修复 NameError 而恢复的 classify_degrees，又按 assistant/post_master 默认 master 且 doctorateRequired=false；一个只写“Research assistant，Salary paid”的通用页面被赋予了实际没有公布的硕士资格。

修改要求：先提取准确实体和 Qualifications 区域，处理否定和备选要求；没有资格证据保持 unknown，不因分类名称补值。用同页两个岗位、标题导航、博士注册不要求、博士学位不要求但注册要求、硕士／博士二选一等样本验收。当前通用解析器未接入主采集，不能将其错误说成所有线上记录已被污染，但接入发现器前必须修复。

## A39 · P1 · 三语审核仍可由有中文标题自动“通过”

位置：`harvest_nine_hei_jobs.py:1004–1041`；`publish.py:45–91`。

当前 18 个岗位的 en/cs 标题全部相同，14 个标 unreviewed，依然进入 published。job_record 只要找到中文标题就给 `translationStatus=verified`，reviewedAt 固定为 2026-09-07，sourceLanguage 固定 en。中文词典补齐并不证明捷克文已经审校，也不能在源文本变化后沿用旧审核时间。

修改要求：按字段／语种记录翻译和审核状态，关联源内容哈希；源资格内容变更必须重新核验。英文、捷克文专名允许保留原名，但不能把整段原文复制视为目标语言审核完成。三语门禁与 A32 同步。MUNI 种子 ID 与中文词典键还不一致（含 sci 与不含 sci），应统一稳定 ID，防止新增岗位自动失去翻译。

## A40 · P2 · 地图提前合并，且原型与正式实现存在差距

位置：`apps/web/src/components/InstitutionMap.svelte:270–363`；`work/map-prototype/EVALUATION.md`。

正式页面确实已使用 MapLibre，但 source.type 是 raster，瓦片来自 `https://tile.openstreetmap.org/{z}/{x}/{y}.png`。并非自托管 PMTiles 矢量地图；浏览器不能动态改写栅格图片中的街道地名。正式配置 cooperativeGestures=false，点位为逐个 DOM Marker，无聚合图层；与评测中“协作手势、聚合已验证”的叙述不同。

用户先前决策是独立原型经中国可达性和真机触屏实测后再替换。仓库评测没有提供可核对的国内运营商／地区／日期、设备型号、请求日志、p95、实际区域瓦片文件和压测记录；不能据此确认已经达标。

评测中“公共 OSM 禁止商业站点调用”的描述不准确。官方要求署名、缓存和适当使用，禁止批量抓取等，并没有全面禁止商业网站；服务可撤回且不保证 SLA。[OSM 官方瓦片政策](https://operations.osmfoundation.org/policies/tiles/)

“全国仅 60–75MB”“15–40ms”“永无访问阻断”“总成本不足 5 元/月”等也缺实际产物／测量／报价依据，应撤下保证式结论，标为未验证估算并列条件。不能用评测文字代替实测。

修改要求：正式地图加开关，未达到门槛时保留已知可用概览；将原型和正式组件共用的配置、手势、聚合行为统一。完成真实数据产物、资源托管清单、三地场景、国内网络和中低端手机实测，再决定正式默认方案。无需为了完成本轮审计擅自购买服务。

## A41 · P2 · 地图断网不降级，键盘点位不可操作

浏览器复现：阻断全部外部请求，25 次 OSM 瓦片请求失败，仍保留 MapLibre 画布和 54 个点；SVG 降级提示为 0。截图显示灰色底图，只有学校点位。当前只捕获初始化异常，没有处理异步瓦片错误／超时，因此“无 WebGL 降级”不能覆盖“中国网络瓦片打不开”。本测试仅模拟网络失败，没有实际测中国网络。

键盘测试：第一个点获得焦点后按 Enter，selected 点为 0；鼠标点击同一点后为 1。Marker 为 role=button 的 div，但只注册 click，没有 Enter／Space 行为。选中还会删除并重建全部 Marker，后续应验证焦点是否丢失。

修改要求：错误／超时可见提示、重试和切换概览／列表；点位使用原生 button 或补齐键盘行为；选中更新保持焦点。补协作手势、同坐标展开、聚合和手机列表视图。页面仍宣称“不使用境外瓦片”，也必须与实际实现同步。

## 下一 Agent 的执行顺序

1. **先修发布隔离和关闭误判：A31–A34。** 这决定错误数据会不会被公开以及有效岗位会不会消失。
2. **再修可靠执行：A35–A36。** 不能把失败标成功，互斥与重试要在并发和故障下成立。
3. **完成真实数据闭环：A37–A39。** 自动发现、准确资格、审核及发布均完成，才扩大来源数量。
4. **地图按约定验收：A40–A41。** 国内实测暂缺时如实标未验证，不阻塞数据可靠性修复，也不宣称正式地图全部完成。

每项修复应保留本轮能够失败的输入作为回归，然后运行现有检查。请更新 PROGRESS 的状态口径，区分“代码已写”“单元测试通过”“闭环验证完成”“生产已部署”。实际本地静态构建成功不等于公开部署成功。

## 本轮证据与边界

项目证据目录：`D:\chatgpt\Czech_Uni_Apply\work\audit3-2026-09-07\`。

- `probes.py`、`probe-results.json`：空快照门禁、非法记录校验、多轮错误下架、503 假成功、否定语义、相邻岗位污染、重试未到期、锁竞态、当前发布岗位统计。
- `browser_probe.py`、`browser-results.json`、`map-tiles-blocked.png`：独立浏览器测试，外部请求均被阻断，未向公共瓦片服务做自动抓取。
- `build.log`：70 测试与 15,314 页构建。

异常测试只改 `work/audit3-2026-09-07/isolated/` 或模拟对象，未改真实来源、快照、调度状态和锁。Python 锁竞态用受控线程交错复现，尚不是完整跨进程压力测试。未进行真实大陆网络、真实移动设备或全站完整 E2E；这些不得标为已验收。临时本地静态服务仅供审计，结束后停止。
