# 捷克留学与带薪科研平台：复审及下一 Agent 执行指导

审计日期：2026-09-07。项目：`D:\chatgpt\Czech_Uni_Apply`。本轮只审计、运行验证、生成交接材料，没有修改业务实现，没有启动真实采集任务或部署。

## 1. 先读与执行边界

先读项目 `AGENTS.md`、根目录 `PROGRESS.md`，再读 `docs/PRODUCT.md`、`UI_RULES.md`、`I18N_RULES.md`、`DATA_MODEL.md`、`CRAWLING_RULES.md`、`REFRESH_POLICY.md`、`ACCEPTANCE.md`、`LANGUAGE_STACK.md`、`DECISIONS.md` 及 `opm/`。不要把旧的 `work/PROGRESS.md` 当成现行入口。

上轮报告：`D:\chatgpt\2026-09-06\https-www-guanfu-online-https-www\outputs\Czech_Uni_Apply_全仓审计_2026-09-07.md`。本文件是增量复审和执行顺序，不替代已有产品规范。工作区已有大量未提交改动，禁止重置、清理或覆盖其他人的工作；开始前重新读取实际文件。

保持已确定的选择：先选英语／捷克语授课，界面语言独立为中、英、捷；不依赖浏览器翻译；公私立及官方查询状态可见；岗位直接进入对应官方申请入口；登记专业不冒充当前招生项目；硕士可申请与是否必须读博分开判断。现有“用学科／城市／学校标签筛选，取消自由搜索框”和语言切换的历史行为是已有记录中的交互决定，不要擅自恢复旧界面。

本轮用户要求下一 Agent 的完善指导，并未授权购买地图服务或公开部署。可以实施本地修复和可验收原型；采购、上线按后续明确授权执行。

## 2. 已验证的改进与检查结果

对照上轮 190 文件摘要，57 个已追踪审计文件发生变化；该数字不含新增文件，不能当成本次全部改动数。新证据和脚本在项目 `work/recheck-2026-09-07/`。

| 检查 | 本次结果 |
|---|---|
| Web 单元测试 | 70/70 通过 |
| Python 采集测试 | 35/35 通过 |
| Go catalog | `go test ./...` 通过，使用缓存结果 |
| Go API | 编译检查通过，包内没有测试 |
| 静态构建 | 成功，15,314 页；不等于生产发布成功 |
| TypeScript 检查 | **失败**：`apps/web/src/lib/catalog.ts:543` 向窄联合类型数组添加 `unknown`，TS2345 |
| Astro check | **失败**：配置加载时 picomatch 的 `require is not defined`，尚未完成组件类型检查 |
| 独立浏览器 | 1440px 专业列表、地图，以及 390px 中文专业／岗位列表，均无 pageerror 和横向溢出；不是所有交互的完整 E2E |
| 专业筛选 | 英语目录 1,273 条；点 Education 后变成 46 条、3 页，结果和 URL 同步，旧死循环未再出现 |

上轮问题复核：显式 closed 优先级、lifecycle closed 阻止申请、跨用户时区判定、公告保留、证据引用、稳定 offering ID、去重音查询在复现中已改善。当前窗口无悬空证据引用，原 inventory ID 无被覆盖删除。代码已增加加载失败提示／重试、日期刷新及三语 404 页面；这些新增路径仍需要下面要求的组件与发布测试。

不要再次报告“关闭岗位仍有官方链接”作为独立错误：中性的原公告链接可以保留。应检查的是是否仍显示“现在申请”以及是否继续列入公开有效岗位。

## 3. 必须优先修复的逻辑问题

### R01 · P1 · Windows 进程锁可能终止正在运行的进程

位置：`services/ingestion/src/worker.py:68–96`。`pid_alive` 调用 `os.kill(pid, 0)`。这不是跨平台存活检查：Windows 上非 CTRL_C／CTRL_BREAK 的信号值会调用 TerminateProcess。锁文件存在且 PID 有效时，第二次运行可能杀死前一个进程；PID 重用还可能命中别的进程。本次仅做代码和官方文档核对，**没有用真实进程执行该函数**。[Python 官方说明](https://docs.python.org/3/library/os.html#os.kill)

行动：使用支持 Windows 的非破坏性进程检查或真正跨平台互斥锁；锁获取必须原子化，不能 exists→write；异常退出应释放自身持有的锁，不能删除别人的新锁。原子写临时文件也应使用每次运行唯一名称。验收：隔离测试启动两个工作进程，仅一个获得锁；原进程保持存活；异常退出和过期锁可恢复。修复前不要启动 worker 的真实运行来“试试看”。

### R02 · P1 · 五天循环尚不能保证每个来源 120 小时内更新

位置：`worker.py:108,201,236`、`refresh_shards.py:28`、`config/refresh-policy.json`。

现状：按排序后的学校索引取模，每天只做当天分片；宕机错过一天不会补采，可能让对应来源间隔变成十天。新增一所排在前面的学校的合成样例使原有 54 校中 53 校改变分片。配置要求岗位 24 小时轻量复核，但实际岗位仍跟学校分片走。重试配置和实际固定等待也不一致，日循环未包裹恢复逻辑。

行动：以持久化 `lastSuccessAt / nextDueAt / retryAt / leaseUntil` 调度来源，分片仅用于限流，不决定是否漏掉过期任务。每次启动补齐到期任务，限制并发和域名速率；岗位状态检查独立运行。稳定哈希或持久分片可避免名单变更大幅扰动，但不能替代到期队列。时间在每次任务执行时注入，移除 harvester 模块级 `TODAY=date.today()`；长驻进程跨日不能仍用启动日。

验收：模拟停机 48 小时、名单插入删除、跨布拉格午夜、任务崩溃、同日重启、重试耗尽；每个未成功来源可追踪，不把“当天分片跑完”写成“全覆盖达标”。如果停机已经突破 SLA，如实记录违约并补采，不能声称算法保证宕机期间仍按时完成。

### R03 · P1 · 岗位爬虫还没有持续发现新岗位

位置：`worker.py:114–116`、`harvest_nine_hei_jobs.py:430,860`。实际入口筛选 `VERIFIED_CANDIDATES`，当前 19 个固定种子；定义了列表页面和通用解析器，不代表主流程会遍历列表发现新岗位。

当前 JSON 为 **18 条、5 个雇主**，其中 post_master 4、assistant 7、postdoc 7；这是记录分类计数，不能解读成 4 个已经充分验证的“硕士无需读博带薪岗”。网页默认筛选显示 11 条，也不能当作完整覆盖数。`PROGRESS.md` 中 19 条是滞后的描述。

行动和验收见第 5 节：必须新增“来源登记→列表分页→详情发现→详情解析→去重→资格核验→版本发布”的闭环，而不是再加几十条手写种子。

### R04 · P1 · 整页提取污染单个岗位，截止日期会误读

位置：`harvest_nine_hei_jobs.py:252–305,792–855,860–940`。

已复现：`Published 2026-09-01. Application deadline: 2026-09-30.` 返回截止日 **2026-09-01**。原因是只判断页面存在 deadline，再对整页找第一个日期。岗位 A 已关闭、岗位 B 仍开放的混合文本，`page_is_closed` 返回 true，主流程把整页文字交给每个候选岗位，存在错误关闭 B 的路径。`Czech is not required` 被当成博士注册“不要求”。薪酬、工作语言仍可继承种子；缺失工作语言默认 `[en]`，不能视为英语工作证据。

行动：先限定岗位详情实体及 Qualifications／Deadline／Salary 等字段范围，正文与页眉、推荐职位、其他公告隔离。优先 JobPosting 等结构化数据，再用站点适配器和标签邻近文本；矛盾数据进入待复核。区分发布日期、申请开放日、开工日期、截止日期。资格支持 MSc./Mgr./Ing. 等真实写法及捷克语，不得仅用英文短语推断。每个关键字段保留来源 URL、片段、定位、抓取时间和解析器版本。链接来源成功不等于外部申请域名已被官方背书。

验收：同页两岗相反状态、发布日期早于截止日期、旧岗位列表、页脚含 PhD、语言 not required、博士优先但非必须、硕士或博士可选资格、只给 MSc.、捷克语要求、工资税前税后及兼职比例均有真实脱敏样本回归。

### R05 · P1 · 多轮申请被合并成一轮

位置：`refresh_shards.py:57` 和 `harvest_nine_hei_jobs.py:864`。窗口按 ownerId 建字典，一个岗位的多个 window 互相覆盖；即使该岗位不在本次分片内，也会丢掉旧轮次。合成 round1+round2 合并后只剩 round2。

行动：以独立 window ID 合并，按 ownerId 分组管理而不是取一条；替换时只更新本次成功解析实体的窗口集合，保留原公告轮次、申请人范围、学年以及状态历史。验收：两岗各三轮，一岗更新一岗失败，另一分片运行，任何未授权删除的轮次均保留。

### R06 · P1 · 数据刷新与网站更新没有闭环

位置：`worker.py:160–231`、`apps/web/src/lib/loadNineHeiJobs.ts:1`、`loadCatalogClient.ts:7–34`。worker 写 source/public JSON；岗位、招生 tracer、学校和申请入口通过构建时 import 进入网页，客户端只取 inventory JSON。worker 没有构建／发布步骤，报告还明确 `published:false`。新爬取的关闭状态不会自动进入已经部署的岗位列表。单独更新 inventory 又可能引入没有静态详情页的新 ID。

行动：选择并记录一种完整发布方式。近期可保持静态站：验证后构建完整不可变快照，成功才原子切换发布版本。或者让列表及详情均读取同一个版本化目录并提供对应路由。禁止“只更新 public 文件”冒充已上线。增加版本清单、校验值、最后成功发布时间、回滚入口及失败不切换规则。对仍停留在旧页面的用户设置合理的版本刷新策略。

验收：模拟新增岗位、关闭岗位、新增专业、回滚和发布失败；三语列表、详情、学校页、收藏、比较页看到一致版本；无新 ID 404；关闭后按既定缓存 SLA 下架，历史直达页仍可解释原因。

### R07 · P1 · 保留旧数据的策略没有覆盖所有采集器

岗位抓取失败保留旧记录已有改进，但 `harvest_apply_portals.harvest_rows` 会在探测全失败时产出 `kind:missing` 和空 URL；`merge_sharded_portals` 仍直接覆盖旧记录。HTTP 成功但课程解析为零也需要异常降幅检查。`job_record` 对已经过期、但未匹配明确关闭语句的岗位返回空对象，失去归档而不只是从列表下架。

行动：分开“抓取失败／无变化／明确已撤下／合法空集合”；失败保留 last-good 值和原 verifiedAt，新增失败时间；关闭改状态不删除记录。汇总成功率须同时涵盖岗位、入口、课程及发布，不能只统计学校 HTTP 成功。

### R08 · P2 · 构建绿灯没有覆盖类型错误和组件行为

修复 `catalog.ts:543` 的字段组类型，显式允许 unknown，不要强制断言掩盖；单独定位 Astro check 的配置依赖加载问题，避免无差别升级全部依赖。CI 必须让类型错误阻止合并／发布。增加真实组件交互验证，现有规则测试无法防止旧的 Svelte 初始化死循环复发。

## 4. 地图为什么不像真实地图，以及推荐方案

现有 `InstitutionMap.svelte` 和 `mapProjection.ts` 使用行政区、河流、湖泊等少量 SVG 路径。最高 72 倍缩放只是放大同一批几何；没有道路、建筑、校园和逐级细节。`preserveAspectRatio="none"` 随容器长宽拉伸，也会影响地理形状。浏览器截图中布拉格点位重叠，不能轻松逐一选择。

本轮直接读取参考站 HTML，确认它加载 **Leaflet 1.9.4 + OpenStreetMap 栅格瓦片**；证据 `work/recheck-2026-09-07/guanfu.html`。这是公开前端观察，不能由此推断它后台用什么爬虫。[参考站](https://www.guanfu.online/)

| 方案 | 适合程度与取舍 |
|---|---|
| **MapLibre GL JS + 自托管捷克区域 OSM 矢量底图／PMTiles** | 长期首选：真实道路和街区，可调地图文字和视觉层级；资源与站点统一托管，避免浏览器直接依赖国外地图 CDN。集成和制图准备比 Leaflet 多，需测手机 GPU／内存。 |
| **Leaflet + 有授权的栅格底图** | 最快得到接近参考站的效果，实现简单；栅格上的语言通常已画死，三语地名切换不如矢量灵活。自托管底图仍有生成、存储与流量成本。 |
| 保留现有 SVG | 适合全国概览或加载失败时降级；不再继续投入“72 倍放大”来替代街道地图。 |
| MAPS.ME | 官方展示重点是离线手机地图。本轮未核实到适合该站、维护状态与商用额度明确的 Web 底图服务，暂不选为核心依赖。不要与另一个产品 Mapme.com 混淆。 |

MapLibre 是浏览器地图渲染库；PMTiles 可用 HTTP Range 按需读取区域档案，不必下载整个国家文件；二者都不是“自动免费提供全球底图的服务”。[MapLibre 文档](https://maplibre.org/maplibre-gl-js/docs/) · [PMTiles 文档](https://docs.protomaps.com/pmtiles/) · [MAPS.ME 官网](https://maps.me/)

不要直接照抄参考站的公共 OSM 瓦片依赖作为中国用户的生产可用性保证。公共服务无 SLA、禁止批量抓取来制造离线包；若自托管，用有许可的原始数据／区域产物生成自己的瓦片，保留署名。[OSM 瓦片政策](https://operations.osmfoundation.org/policies/tiles/)

下一步先做 MapLibre 的独立可切换原型，默认二维道路图、关闭倾斜和三维建筑。用户想要的真实感首先来自道路／公园／街区，不必为卫星影像额外付费。数据、style、glyphs、sprites、字体全部纳入自托管清单；中文覆盖不能只翻译按钮。地名优先选当前语言的已存在名称，缺少时回退当地名并可查看中文别名，不能假造“已翻译”。学校名称用本项目审校词典。

必须实现：国家→城市→校园逐级细节，点聚合及同坐标列表，选学校联动卡片，筛选后 fitBounds，键盘可选学校，移动端地图／列表切换、双指缩放、页面滚动不被地图无条件截获。区分“登记地址／校本部”和“实际授课／工作地点”，不可把一个总部点伪装成所有校区。地图不能成为查看学校的唯一入口。

验收：布拉格、布尔诺、俄斯特拉发三个城市，以及全国视图；三语、390px、键盘、WebGL 不可用、瓦片超时、同坐标、多点过滤。地图分包按进入页面／可视区懒加载，不增加专业列表首屏负担。区域档案要有边界和 zoom 预算，禁止一开始生成全球高精度底图。成本按实测瓦片大小、Range 请求数、缓存命中率和访问量估算；中国大陆可达性须用当地网络实测，欧洲本机成功不能替代。

## 5. 岗位覆盖与采集策略的目标设计

### 有实际漏项，不能承诺“全部岗位”

当前五个雇主的岗位文件没有 MUNI。官方抽查发现 MUNI 的 **Ph.D. Fellowship（81652）**，截止 2026-09-20，要求 MSc.，明确 0.5 FTE 雇佣并属于四年博士项目；应该进入“硕士可申请、需要读博”的带薪类别，不能标作无需博士注册的研究助理。金额仍需进一步核查，不得把混合资助全部标为工资。[官方详情](https://www.muni.cz/en/about-us/careers/vacancies/81652)

同一学院官网还有截止 2026-09-30 的天体物理博士后岗位；这是平台博士后类别的漏项例子，不能拿它充当“硕士后”需求已覆盖的证据。[官方招聘列表](https://www.sci.muni.cz/en/careers-at-the-sci-muni/open-positions-at-the-sci-muni) · [博士后详情](https://www.muni.cz/en/about-us/careers/vacancies/81445)

科研机会的雇主可进一步纳入捷克科学院及其研究所，独立于大学基线统计；不要把研究所硬塞进大学公私立或中留服学校认证字段。[科学院招聘入口](https://www.avcr.cz/en/about-us/career/selection-procedures/)

### 实施链条

1. **来源登记**：以现有 54 校基线作核对起点，补充校级招聘、院系、实验室和被官网链接的 ATS；登记来源类型、语言、分页方式、负责适配器、lastSuccessAt、覆盖范围。54 是当前本地基线数，不是永远不变的法定总数。学院与研究所的漏项需要单独衡量。
2. **发现**：官方 RSS／sitemap／公开接口／招聘列表优先，跟随分页和正式详情链接；EURAXESS 等可提供线索，回官方核验。发现新 ID 后自动入候选，不需要改源码常量。
3. **采集**：轻量 HTTP 优先，ETag／Last-Modified、本轮 URL 缓存、每域限速；困难公开页面使用已安装 Scrapling，执行路径可配置，避免把个人 Python 安装路径写死。适配器明确普通抓取→Scrapling→失败保留旧版的升级条件；已有个别招生脚本使用 Scrapling 不代表所有岗位来源都已接入。
4. **资格与薪酬**：拆开 minimumDegree、doctorateRequired、doctoralEnrollment、paidStatus、salary、contractType、FTE、workingLanguages。需要博士学位的岗位不能进入硕士入口；要求注册博士但允许硕士申请的职位单独可见；资格不清楚保留待核实，不因标题 assistant／R1 猜测。正式雇佣、奖学金、混合资助分开。
5. **时效**：学校及完整发现每 120 小时一轮，活动岗位轻量复核目标 24 小时；临近截止可提高频率。已知截止可本地按布拉格时区过期，不等网页重新爬取；官方提前关闭只能在下次成功核验后得知，明确系统的检测延迟。
6. **状态与申请**：保留 closed／expired 历史；超时、403、429、500 或孤立 404 不能直接等于关闭。记录深层申请 URL／官方操作说明，核对重定向的 ATS 域名；主标题或清晰主按钮直接到该岗位的正式入口，没有深链接时明确“查看申请说明”，不要把学校首页称作直接申请。
7. **发布**：执行 R06 的版本化发布，采集、验证、翻译审校、发布各有状态和时间。至少记录来源成功率、到期未采数、发现新岗位数、未验证资格数、未知薪酬数、实际发布延迟。

覆盖验收：先完成 MUNI 这一条从发现到页面显示的纵向样例，再推广适配器。对基线中的每校都有“来源已映射／暂无岗位／待接入／本轮失败”状态；从不同大小学校、院系和研究所抽取人工清单，核对召回率和误收率。发布结果写明样本日期、分母和未覆盖来源，不能用“爬取了 54 个学校主页”证明岗位齐全。

## 6. 页面交互和用户体验的后续完善

### 已有优点应保留

授课语言与界面语言分开；筛选即时生效；手机有筛选抽屉；申请入口与站内说明分开；缺失学费不显示免费；中留服显示查询参考而非认证保证。浏览器样例无横向溢出。不要为“重做美观”推倒这些有效机制。

### U01 · P1 · 内容三语化不完整

本轮目录中 5,019 个 offering 有 5,015 个标题三语字段相同；18 个岗位标题全部相同。相同字符串本身不等于所有专名都应翻译，但当前中文卡片确有整段英文／捷克文标题，足以证明内容层的三语要求未完成。不得仅复制原文填满三列就宣告通过。

增加 originalText／sourceLanguage／translationStatus／reviewedAt；专业和岗位用中文易懂名称配原文，保留原资格正文链接。全站通用提示和关键决策字段必须完成三语；动态文本的未审校状态显式呈现，发布门禁遵守现有 I18N 规则。

### U02 · P2 · 信息密度与可操作性

中文岗位列表 390px 样例 11 张卡形成约 6,646px 长页，卡片重复小徽章和提示；首屏多段采集／验证术语占用注意力。将稳定说明集中到“数据说明”，卡片优先呈现：岗位中文名＋原名、雇主／城市、硕士资格、是否需读博、薪酬类型、截止／轮次、申请按钮、最近核验。保留必要解释，但不要把 tracer、Wikidata、坐标匹配方法等工程词汇铺在普通用户主流程。

学校选项五十余个全铺标签不易扫描：保持现有标签交互方向，优先城市联动、可折叠更多学校、选中条件摘要、清空单项、零结果建议；是否增加标签内检索作为可选设计，不能擅自恢复已取消的全局自由搜索。手机抽屉验证焦点进入／退出、Esc、背景滚动和返回结果后的滚动位置。

### U03 · P2 · 状态呈现还有局部不一致

`LiveWindows.svelte` 的申请按钮已使用 wholeClosed 阻断，但每轮徽章仍直接 `evaluateWindow`，可出现整岗关闭、某轮显示开放的矛盾。明确显示“岗位整体已关闭；以下是历史轮次”。

`clock.ts` 用 Date.parse 猜边界，与业务的 Europe/Prague 日期规则不同，且有未来时间时最长一小时才刷新。日期级截止是该当地日结束，不是 UTC 当日零点。改用共享业务边界函数，并设最大 60 秒活跃页刷新；测试跨午夜、夏令时、后台恢复和数据加载后重排定时器。学校页／收藏／比较页也需同一时间与版本规则，不能仅列表正确。

### U04 · P2 · 应用型教育数据仍缺失

当前应用／实践导向筛选实际为 0。不要在页面造成“捷克没有此类项目”的印象，显示尚未完成核验的覆盖状态。根据捷克原有机构类别及具体项目职业／学术导向证据建字段，不把德国 FH 名称机械移植，也不能把私立直接等同应用型。补来源后再评价覆盖。

## 7. 下一 Agent 的实施顺序与完成定义

**第一批：阻断错误传播。** R01、R04、R05、R07、R08。先加入能失败的最小回归样例，再修复；不扩大到整体技术栈重写。

**第二批：数据闭环。** R02、R03、R06。以 MUNI 官方招聘为贯通样例，覆盖发现、工资／资格证据、三语内容、申请链接、关闭与发布；再推广学校和院系来源。

**第三批：地图和 UX。** MapLibre 自托管底图原型、U01–U04。原型先验证地图真实细节和中国访问条件，再决定部署资源；不要购买服务来代替技术验证。

语言与成本：沿用 Astro／Svelte／TypeScript 页面、Go 在线 API、Python＋Scrapling 离线采集。网页启动速度主要取决于资源量、CDN、客户端计算和网络，不因 Go／Rust 名称自动变快。现阶段不要为少量离线解析重写 Rust。队列状态小规模可先用 SQLite；需要多人维护、审核和发布事务时衔接已有 PostgreSQL／Supabase 设计，不能把静态快照发布问题误当作“换数据库就解决”。本轮未重新核价，不把任何服务永久免费作为预算承诺。

每批完成需交付：修改路径、行为变化、测试结果、剩余限制、更新后的 PROGRESS；涉及对象／过程／状态变更，同步 OPM DOT 和 OPL，遵守项目 ISO 19450 子集及既有图例。不得把本项目自定义 DOT 表达宣称为完整标准认证。

最终 DoD：现有检查与新增回归全部通过；无类型错误；三语关键任务流可独立使用；真实新岗位可自动发现并正确分类；旧版本失败不丢数据；岗位关闭在声明 SLA 内从各公开列表消失；轮次历史不丢；地图三个城市有真实街道细节；手机、键盘和降级路径可用；报告覆盖范围而非笼统“全量”。

## 8. 证据、复现及 suggested skills

本轮证据目录：`D:\chatgpt\Czech_Uni_Apply\work\recheck-2026-09-07\`。

- `recheck.py`／`recheck-results.json`：日期、资格、多轮覆盖、分片扰动、当前岗位计数、旧摘要变化。
- `logic_audit.mjs`／`logic-results.json`：上轮纯逻辑复现的重跑。注意 `schoolListExpiry.inSchoolHTML` 是构建时 HTML 文本，不能单独证明跨日客户端仍显示；原脚本部分旧假设已在本报告中限定。
- `browser_recheck.py`／`browser-results.json`／四张截图：本轮新构建的界面样例。脚本使用独立本地静态服务 4327，不用用户登录状态；再跑时先启动该服务。
- `build.log`：70 测试及 15,314 页构建。
- `guanfu.html`：参考站公开前端源码，仅用于核验，不复制其设计资产。

项目根目录可运行 `py -3 work/recheck-2026-09-07/recheck.py`；Web 目录运行 `npm run build`、`npx tsc --noEmit`、`npm run check`；Python 测试 `py -3 -m pytest services/ingestion/tests -q`。Go 在 `services/catalog` 和 `apps/api` 分别运行 `go test ./...`。不要用本文件替代对工具当前实际输出的判断。

Suggested skills：定位组件／调度缺陷可用 `diagnose`，复杂回归如用户选择测试先行可用 `tdd`，地图方案比较可用 `prototype`，阶段完成可用 `handoff`。使用这些技能前按各自说明读 SKILL.md；需要项目技能配置的先执行其 setup 要求。不是要求所有技能都调用，也不是要求启动其他 Agent。
